# -----------------------------------------------------------------------------
# .env.example — 环境变量模板（提交 Git；Next 不会加载此文件）
# -----------------------------------------------------------------------------
# 使用：在 slack-frontend 目录执行
#   cp .env.example .env.local
# Windows（cmd）: copy .env.example .env.local
# Windows（PowerShell）: Copy-Item .env.example .env.local
# 然后编辑 .env.local。.env.local 已被 git 忽略，勿提交密钥。
# 说明：https://nextjs.org/docs/app/building-your-application/configuring/environment-variables
# -----------------------------------------------------------------------------

# 开发服务器额外允许的 Origin（逗号分隔，无引号）。供 next.config.mjs → allowedDevOrigins。
# 不配置则由 next.config 内默认值生效；请勿使用「空 = 允许所有」的误解。
# ALLOWED_DEV_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# 浏览器端可读，必须以 NEXT_PUBLIC_ 开头；勿把密钥写成 NEXT_PUBLIC_。
# NEXT_PUBLIC_APP_URL=http://localhost:3000
