# =============================================================================
# .env —— 全环境基准（任意模式都会参与合并）
# =============================================================================
# 何时加载：next dev、next build、next start 等命令执行时都会读入（与 NODE_ENV 无关，
#            该文件始终作为「底座」参与变量合并）。
#
# 怎么用：
#   - 放团队统一的、可提交仓库的默认值（非密钥）。
#   - 个人机器差异、密钥、本机 IP 等 → 写在 .env.local（已被 .gitignore，勿提交）。
#
# 覆盖顺序（后加载的覆盖先加载的，同一模式内）：
#   .env → .env.[development|production|test] → .env.local → .env.[mode].local
#   详见：https://nextjs.org/docs/app/building-your-application/configuring/environment-variables
#
# 安全：勿在此文件写 API 密钥、数据库密码；密钥只放在 .env.local 或 CI/托管平台环境变量。
# =============================================================================

# 可选：开发服务器额外允许的 Origin（逗号分隔）。供 next.config.mjs → allowedDevOrigins 使用。
# 不配置或留空时，使用 next.config.mjs 内的代码默认值。
# ALLOWED_DEV_ORIGINS=http://127.0.0.1:3000
